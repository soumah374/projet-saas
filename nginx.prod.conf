worker_processes auto;

events {
  worker_connections 1024;
}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;
  sendfile on;
  keepalive_timeout 65;

  map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
  }

  # Use Docker embedded DNS for runtime name resolution
  resolver 127.0.0.11 valid=30s;

  # HTTP server (no SSL - for development/testing)
  server {
    listen 80;
    server_name _;

    # Frontend (SPA) - static files from /var/www/html
    location / {
      root /var/www/html/projet-saas/frontend/dist;
      try_files $uri $uri/ /index.html;
      expires 1h;
      add_header Cache-Control "public, max-age=3600";
    }

    # API proxy to backend
    location /api/ {
      # Force runtime DNS resolution by using a variable
      set $backend_host backend;
      proxy_pass http://$backend_host:8000;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_redirect off;
    }

    # WebSocket proxy for chat/realtime features
    location /ws/ {
      # Force runtime DNS resolution for websocket upstream
      set $backend_host backend;
      proxy_pass http://$backend_host:8000;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_connect_timeout 86400s;
      proxy_read_timeout 86400s;
      proxy_send_timeout 86400s;
    }

    # Health check
    location /health {
      access_log off;
      return 200 "OK";
      add_header Content-Type text/plain;
    }
  }
}
